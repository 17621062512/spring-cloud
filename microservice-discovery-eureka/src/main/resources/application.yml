server:
  port: 8888

eureka:
  client:
    #是否要注册到其他eureka server实例(默认为true)
    register-with-eureka: false
    #是否从其他eureka server实例获取数据(默认为false)
    fetch-registry: false
    #指定服务注册中心地址
    service-url:
      defaultZone: http://localhost:8888/eureka/

